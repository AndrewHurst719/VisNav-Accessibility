<Window x:Class="VisNav.App.CrosshairOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        IsHitTestVisible="False"
        ResizeMode="NoResize"
        Focusable="False"
        Title="VisNav Crosshair">
    <Canvas x:Name="RootCanvas" IsHitTestVisible="False">
        <!-- Outline ring (only shown for the White-on-black preset), drawn behind. -->
        <Ellipse x:Name="OutlineRing" Fill="Transparent" Visibility="Collapsed" IsHitTestVisible="False" />
        <!-- Primary ring. -->
        <Ellipse x:Name="Ring" Fill="Transparent" IsHitTestVisible="False" />
    </Canvas>
</Window>
